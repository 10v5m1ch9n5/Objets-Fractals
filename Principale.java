public class Principale {
	//~ public static Menu menu;
	public static void main(String[] args){
		Menu menu = new Menu();
		//~ Dimension screenSize = Toolkit.getDefaultToolkit().getScreenSize();
		
		//~ RotationJulia rj = new RotationJulia(0.7885, 300, (int)screenSize.getWidth(), (int)screenSize.getHeight());
		//~ PlanComplexe pc = new PlanComplexe(rj, (int)screenSize.getWidth(), (int)screenSize.getHeight(), 0);
	}
}
